name: flutter_charts_poc
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  dartx: ^1.2.0
  equatable: ^2.0.5
  fl_chart: ^0.68.0
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.5
  flutter_staggered_grid_view: ^0.7.0
  flutter_svg: ^2.0.10+1
  go_router: ^14.1.4
  google_fonts: ^5.1.0
  package_info_plus: ^5.0.1
  universal_platform: ^1.1.0
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
